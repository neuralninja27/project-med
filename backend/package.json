{
  "name": "backend",
  "version": "1.0.0",
  "description": "The backend of project med",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd .. && npm start",
    "dev": "concurrently -n 'Server,Client' -c 'cyan,purple' \"nodemon server.js\" \"npm run client\""
  },
  "author": "Pranav Soni",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.12.13"
  }
}
